# --- Núcleo de Inteligencia Artificial (PyTorch) ---
# Nota: Si tienes tarjeta gráfica NVIDIA, ver instrucciones abajo para versión CUDA
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# --- Visión por Computador (YOLO) ---
ultralytics>=8.0.0
# Usamos la versión con GUI para que puedas ver las ventanas del juego en local
opencv-python>=4.8.0  
pillow>=9.0.0

# --- Audio y Voz (Whisper + TTS) ---
# Importante: El paquete oficial en PyPI es 'openai-whisper', no 'whisper'
openai-whisper
gTTS>=2.3.0
soundfile>=0.12.1

# --- Utilidades y Entorno ---
numpy>=1.22.0
ipython  # Necesario si usas el código que importaba IPython.display
requests # Útil para descargar modelos o imágenes de prueba
scipy    # A veces requerido internamente por librerías de audio