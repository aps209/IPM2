El archivo requirements.txt contiene todas las dependencias necesarias para el correcto funcionamiento de la aplicación

Ejecutar con `pip install -r requirements.txt
`

Una vez ejecutado el codigo desde la celda del notebook, abrir el enlace a localhost que proporciona gradio.
